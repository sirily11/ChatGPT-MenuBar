import XCTest
@testable import MarkdownRenderingPlugin

final class MarkdownRenderingPluginTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MarkdownRenderingPlugin().text, "Hello, World!")
    }
}
